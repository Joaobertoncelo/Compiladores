package test.entities;

public class ExprNodeTest {
    //Como ExprNode é uma classe abstrata e não pode ser instanciada diretamente, 
	//não precisamos de testes diretos para ela. Os testes das subclasses 
	//(NumNode e OpNode) cobrem o comportamento necessário.
}
